---
name: {{.Names.Skill}}
description: {{if eq .Preset.Name "binary-analysis"}}Linked binary-analysis skill for static triage, evidence collection, review, and workflow handoff.{{else}}Linked starter skill for planning, evidence collection, review, and handoff inside a generated kit workflow.{{end}}
version: 1.0.0
author: GoFlow Studio
format: goflow
mode: chat
preferred_agent: {{.Names.Agent}}
allowed_tool_kinds: [read]
output_kind: {{if eq .Preset.Name "binary-analysis"}}structured_binary_handoff{{else}}structured_handoff{{end}}
priority: 50
max_iterations: 4
tools:
{{ range .SkillTools }}
  - name: {{.Name}}
    required: {{.Required}}
{{ end }}
params:
  - name: request
    type: string
    description: User task or workflow stage request.
    required: true
  - name: scope
    type: string
    description: Bounded domain, repository, target, or operating scope.
    required: false
  - name: upstream
    type: string
    description: Compact output from an earlier workflow node.
    required: false
activation:
  keywords: [{{.Names.Kit}}, {{.Names.Skill}}, starter-kit, workflow-handoff]
  embedding_description: Use for generated starter kit workflows that need planning, evidence, review, and concise downstream handoff.
metadata:
  kit: {{.Names.Kit}}
  generated_agent: {{.Names.Agent}}
  generated_tool: {{.Names.Tool}}
  generated_workflow: {{.Names.Workflow}}
  generated_policy_rule: {{.Names.PolicyRule}}
---
{{ if eq .Preset.Name "binary-analysis" }}
## Role

Operate as a reusable binary-analysis workflow skill for this kit. Produce compact, structured outputs that can be passed to the next workflow node.

## Workflow

1. Restate the current task, scope, assumptions, and missing inputs.
2. If the request references a binary or opaque artifact, use binary_file_info first, then binary_strings, and hex_preview when offsets or headers need a closer look.
3. Use read_text only for analyst notes, reports, decompiled text, or other UTF-8 text artifacts.
4. Emit clear sections for Findings, Decisions, Risks, Next Inputs, and Acceptance Criteria.
5. Keep large raw evidence out of the final answer; reference artifact IDs or file paths instead.
6. Do not claim runtime behavior or exploitability without enough static evidence, and do not execute untrusted binaries.
{{ else }}
## Role

Operate as a reusable workflow skill for this kit. Produce compact, structured outputs that can be passed to the next workflow node.

## Workflow

1. Restate the current task, scope, assumptions, and missing inputs.
2. Collect only the evidence required for the current stage. Use the linked read-only helper tool when a workspace file must be inspected.
3. Emit clear sections for Findings, Decisions, Risks, Next Inputs, and Acceptance Criteria.
4. Keep large raw evidence out of the final answer; reference artifact IDs or file paths instead.
5. When blocked, state the exact missing input or approval required so the workflow can route correctly.
{{ end }}

## Example Request

> {{if .Preset.Examples}}{{(index .Preset.Examples 0).Request}}{{else}}Create a scoped plan and final handoff.{{end}}
