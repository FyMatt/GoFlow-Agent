# Loaded automatically from configs/providers/*.yaml.
primary:
    provider: openai-compatible
    base_url: ${GOFLOW_BASE_URL}
    api_key: ${GOFLOW_API_KEY}
    model: ${GOFLOW_MODEL}
    provider_message_fields:
        - reasoning_content
    timeout: 60s
    retry_backoff: 2s
